package it.pierpalab.hastascrape.dao;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class DaoDB extends DAO {
	private static final Logger log = LogManager.getLogger(DaoDB.class);
}
